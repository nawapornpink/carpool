# ระบบจองรถ (Carpool Management System)

ระบบนี้เป็นระบบจองรถสำหรับหน่วยงาน ใช้สำหรับบริหารจัดการการใช้งานรถภายในองค์กร ตั้งแต่การจองรถ การบันทึกเลขไมล์ การเติมน้ำมัน ไปจนถึงการตรวจสอบข้อมูลรายเดือนและการออกรายงาน

เอกสารนี้เป็น **README ภาพรวม** เพื่อให้ผู้ที่รับช่วงต่อเข้าใจโครงสร้างและแนวทางการใช้งานระบบโดยรวม หากต้องการรายละเอียดเชิงลึก ให้ดูเอกสารแยกตามบทบาทด้านล่าง

---

## ภาพรวมความสามารถของระบบ

* จองรถและจัดการสถานะการใช้งาน
* บันทึกเลขไมล์ก่อน–หลังการใช้งานรถ
* บันทึกการเติมน้ำมันแบบผูกกับการจอง (case by case)
* ตรวจสอบข้อมูลการใช้งานรายเดือน
* ดาวน์โหลดรายงานในรูปแบบ Word และ Excel

ระบบพัฒนาด้วย **Django Framework (Python)** และใช้ฐานข้อมูลเริ่มต้นเป็น **SQLite (`db.sqlite3`)**

---

## กลุ่มผู้ใช้งาน

### 👤 ผู้ใช้งานทั่วไป (EMP)

* จองรถ
* เริ่มใช้งาน / คืนรถ
* บันทึกการเติมน้ำมัน
* ดูประวัติการใช้งานของตนเอง

### 🛠 ผู้ดูแลระบบ (ADM)

* จัดการข้อมูลรถและผู้ใช้งาน
* ตรวจสอบข้อมูลรายเดือน
* แก้ไขข้อมูลเลขไมล์ / การเติมน้ำมัน
* ดาวน์โหลดรายงานสรุป

---

---

## เทคโนโลยีที่ใช้

* Python 3.10+
* Django Framework
* SQLite (เริ่มต้น)
* Bootstrap / HTML / CSS / JavaScript
* python-docx (รายงาน Word)
* openpyxl (รายงาน Excel)

---

## โครงสร้างโปรเจกต์และหน้าที่ของไฟล์ (Project Structure & File Description)

ส่วนนี้อธิบาย **ไฟล์/โฟลเดอร์สำคัญทั้งหมด** ตามโครงสร้างที่ใช้งานจริงในโปรเจกต์ เพื่อให้ผู้รับช่วงต่อรู้ว่า “ไฟล์ไหนทำอะไร” และควรแก้ตรงไหนเมื่อมีปัญหา

> หมายเหตุ: โฟลเดอร์/ไฟล์ที่ขึ้นต้นด้วย `__pycache__` เป็นไฟล์ cache ที่ Python สร้างเอง ไม่จำเป็นต้องแก้ไขและไม่ควร commit

```text
CARPOOL/
│ manage.py
│ db.sqlite3
│ .venv/
│
├─ carpool_site/
│   ├─ __init__.py
│   ├─ settings.py
│   ├─ urls.py
│   ├─ asgi.py
│   └─ wsgi.py
│
└─ booking/
    ├─ __init__.py
    ├─ admin.py
    ├─ apps.py
    ├─ forms.py
    ├─ models.py
    ├─ tests.py
    ├─ urls.py
    ├─ views.py
    │
    ├─ migrations/
    ├─ management/
    ├─ report_templates/
    │   ├─ car_report_template.docx
    │   └─ fuel_report_template.xlsx
    │
    ├─ scripts/
    │   ├─ __init__.py
    │   ├─ audit_odometer.py
    │   └─ seed_sample_bookings.py
    │
    ├─ services/
    │   ├─ __init__.py
    │   ├─ report_car_docx.py
    │   └─ report_fuel_excel.py
    │
    ├─ static/booking/
    │   ├─ css/
    │   └─ img/
    │
    ├─ templates/booking/
    │   ├─ base.html
    │   ├─ login.html
    │   ├─ admin_dashboard.html
    │   ├─ admin_booking_list.html
    │   ├─ admin_booking_detail.html
    │   ├─ admin_booking_edit.html
    │   ├─ admin_cars.html
    │   ├─ admin_car_form.html
    │   ├─ admin_employees.html
    │   ├─ admin_employee_form.html
    │   ├─ admin_fuel_refill_edit.html
    │   ├─ admin_monthly_audit.html
    │   ├─ admin_monthly_report.html
    │   ├─ user_dashboard.html
    │   ├─ user_calendar.html
    │   ├─ user_booking_form.html
    │   ├─ user_return_car.html
    │   └─ user_fuel_refill.html
    │
    ├─ templatetags/
    │   ├─ __init__.py
    │   ├─ booking_extras.py
    │   └─ form_extras.py
    │
    └─ utils/
        └─ __init__.py
```

---

### ไฟล์ระดับโปรเจกต์ (Root)

* **`manage.py`**: จุดสั่งงานหลักของ Django (เช่น `runserver`, `migrate`, `shell`, `createsuperuser`, `collectstatic`)
* **`db.sqlite3`**: ฐานข้อมูลหลักของระบบ (SQLite) เก็บข้อมูลรถ/การจอง/เติมน้ำมัน/ผู้ใช้ ฯลฯ
* **`.venv/`**: Virtual Environment (เก็บ Python packages เฉพาะโปรเจกต์) ไม่ควรแก้ไขและไม่ควร commit

---

### โฟลเดอร์ `carpool_site/` (Project Config)

* **`settings.py`**: การตั้งค่าหลักของระบบ (DATABASE, INSTALLED_APPS, STATIC, TIMEZONE, DEBUG, ALLOWED_HOSTS)
* **`urls.py`**: route ระดับโปรเจกต์ (เชื่อมไปยัง `booking/urls.py` และ path อื่น ๆ)
* **`wsgi.py`**: entry point สำหรับการ Deploy แบบ WSGI (เช่น Waitress บน Windows)
* **`asgi.py`**: entry point สำหรับ ASGI (กรณีใช้งาน async/websocket ในอนาคต)
* **`__init__.py`**: บอกว่าโฟลเดอร์นี้เป็น Python package

---

### โฟลเดอร์ `booking/` (App หลักของระบบ)

#### ไฟล์หลัก

* **`models.py`**: โครงสร้างฐานข้อมูล (เช่น Car, Booking, FuelRefill, Profile) และความสัมพันธ์ของข้อมูล
* **`views.py`**: Logic การทำงานทั้งหมดของระบบ (หน้า user/admin, คืนรถ, เติมน้ำมัน, ตรวจรายเดือน, export รายงาน)
* **`urls.py`**: route ภายในแอป booking (mapping URL → view)
* **`forms.py`**: Django Forms สำหรับหน้าเพิ่ม/แก้ไขข้อมูลต่าง ๆ
* **`admin.py`**: การตั้งค่า Django Admin (เช่น แสดง list, filter, search ของแต่ละ model)
* **`apps.py`**: config ของ app `booking`
* **`tests.py`**: ไฟล์สำหรับเขียน unit tests (ถ้าขยายระบบในอนาคต)

#### `migrations/`

* เก็บไฟล์ migration ของ Django (ประวัติการเปลี่ยนแปลงโครงสร้างฐานข้อมูล)
* โดยทั่วไป **ไม่ควรลบ** เว้นแต่รีเซ็ตระบบจากศูนย์และเข้าใจผลกระทบ

#### `management/`

* โฟลเดอร์สำหรับ custom management commands (ถ้ามีในอนาคต)
* ปัจจุบันอาจยังไม่ถูกใช้งานมาก แต่โครงสร้างรองรับไว้แล้ว

---

### `booking/report_templates/` (Template สำหรับออกรายงาน)

* **`car_report_template.docx`**: Template รายงาน Word (รายงานการใช้รถ)
* **`fuel_report_template.xlsx`**: Template รายงาน Excel (รายงานการใช้น้ำมัน)

> จุดสำคัญ: ถ้ารูปแบบเอกสารต้องปรับ (หัวตาราง/เส้น/ฟอนต์/ตำแหน่ง) มักจะแก้ที่ไฟล์ template เหล่านี้ร่วมกับโค้ดใน `booking/services/`

---

### `booking/services/` (โค้ดสร้างรายงาน)

* **`report_car_docx.py`**: สร้างไฟล์ Word จาก template + mapping token (แทนค่า {{...}}) และคืนค่าเป็น BytesIO
* **`report_fuel_excel.py`**: สร้างไฟล์ Excel จาก template + เติมข้อมูลเป็นตาราง สรุปยอด และคืนค่าเป็น BytesIO

> โดยทั่วไป view ใน `booking/views.py` จะเรียก service เหล่านี้เพื่อสร้างไฟล์ แล้วตอบกลับเป็น download

---

### `booking/scripts/` (สคริปต์ช่วยงาน/ข้อมูลตัวอย่าง)

* **`seed_sample_bookings.py`**: สคริปต์สร้างข้อมูลตัวอย่าง (ไว้ทดสอบ/เดโม)
* **`audit_odometer.py`**: สคริปต์ตรวจสอบเลขไมล์/ความผิดปกติ (ใช้ช่วยเช็กข้อมูล)

> หมายเหตุ: สคริปต์กลุ่มนี้เหมาะใช้ในช่วงพัฒนา/ตรวจข้อมูล ไม่จำเป็นต้องใช้ในงานประจำวันของผู้ใช้ทั่วไป

---

### `booking/templates/booking/` (หน้าเว็บทั้งหมด)

* **`base.html`**: layout หลัก (navbar, theme, include css/js) ทุกหน้า extends ไฟล์นี้
* **`login.html`**: หน้าเข้าสู่ระบบ

**ฝั่ง Admin**

* `admin_dashboard.html`: หน้าแดชบอร์ดแอดมิน
* `admin_booking_list.html`: รายการจองทั้งหมด
* `admin_booking_detail.html`: ดูรายละเอียดการจอง
* `admin_booking_edit.html`: แก้ไขข้อมูลการจอง
* `admin_cars.html` / `admin_car_form.html`: จัดการรถ
* `admin_employees.html` / `admin_employee_form.html`: จัดการผู้ใช้งาน
* `admin_fuel_refill_edit.html`: แก้ไขข้อมูลเติมน้ำมัน
* `admin_monthly_audit.html`: ตรวจสอบข้อมูลรายเดือน + ดาวน์โหลดรายงาน
* `admin_monthly_report.html`: หน้าสรุปรายเดือน (ถ้ามี)

**ฝั่ง User**

* `user_dashboard.html`: หน้าแดชบอร์ดผู้ใช้
* `user_calendar.html`: ปฏิทินการจอง
* `user_booking_form.html`: ฟอร์มจองรถ
* `user_return_car.html`: คืนรถ/กรอกเลขไมล์หลังใช้
* `user_fuel_refill.html`: บันทึกเติมน้ำมันของทริป

---

### `booking/static/booking/` (ไฟล์หน้าตาเว็บ)

* `css/`: ไฟล์ธีม/สไตล์ (เช่น สี, spacing, ปุ่ม, table)
* `img/`: รูปภาพ (เช่น โลโก้ PEA, icon)

> ถ้าต้องปรับธีมสี/ความสวยงาม ส่วนใหญ่แก้ใน `static/booking/css/` และ `templates/booking/base.html`

---

### `booking/templatetags/` (ฟังก์ชันช่วยใน Template)

* **`booking_extras.py`**: filter/tag ที่ใช้ใน template เช่น ฟอร์แมตวันที่, สถานะ, การแสดงผล
* **`form_extras.py`**: helper สำหรับฟอร์มหรือการแสดง error/label ให้สวยขึ้น

---

### `booking/utils/`

* โฟลเดอร์เตรียมไว้สำหรับฟังก์ชันช่วยงานร่วม (utility functions)
* ถ้ามีฟังก์ชันที่ถูกใช้หลายไฟล์ สามารถย้ายมารวมที่นี่เพื่อให้ดูแลง่าย

---

## การเริ่มต้นใช้งาน (Quick Start)

````---

## การเริ่มต้นใช้งาน (Quick Start)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver


เข้าใช้งานที่

```
http://127.0.0.1:8000/
```

รายละเอียดเพิ่มเติมดูที่ **README_ADMIN.md**

---

## หมายเหตุ

* ระบบนี้ออกแบบมาเพื่อใช้งานภายในหน่วยงาน
* ก่อนนำไปใช้งานจริง ควรปิด `DEBUG = False`
* แนะนำสำรองข้อมูลเป็นระยะ โดยเฉพาะไฟล์ `db.sqlite3`

---

## การติดตั้งและ Deploy บน Windows (Developer + Production)

ส่วนนี้เป็นคู่มือ **รวม** สำหรับทีมพัฒนา/ทีมดูแลระบบที่ต้องการติดตั้งบน Windows (รวมถึง Windows Server 2020) ตั้งแต่เริ่มต้นจนถึง Deploy ให้รันเป็นบริการได้

> หมายเหตุ:
> - ขั้นตอนด้านล่างใช้แนวทาง Deploy แบบ **Waitress (WSGI)** ซึ่งเหมาะกับ Windows
> - ถ้าองค์กรต้องการใช้ IIS เป็น Reverse Proxy สามารถทำเพิ่มภายหลังได้ (แนะนำสำหรับ Production)
> - เริ่มต้นระบบใช้ SQLite ได้ แต่ Production จริงแนะนำให้ย้ายไป PostgreSQL/SQL Server ตามนโยบายองค์กร

### 0) สิ่งที่ต้องเตรียม

- Windows 10/11 หรือ Windows Server 2020
- Python **3.10+** (แนะนำ 3.10/3.11)
- Git (ถ้าดึงโค้ดจาก repo)
- สิทธิ์ Administrator (กรณีตั้ง service/เปิดพอร์ต/ตั้ง firewall)

---

## A) ติดตั้งแบบ Developer (รันด้วย runserver)

### 1) ติดตั้ง Python
1. ดาวน์โหลดและติดตั้ง Python 3.10+  
2. ตอนติดตั้งให้ติ๊ก **Add Python to PATH**  
3. เปิด PowerShell แล้วเช็ค

```powershell
python --version
pip --version
```

### 2) ดึงโค้ด/แตกไฟล์โปรเจกต์
- ถ้าใช้ Git:

```powershell
git clone <repo-url>
cd CARPOOL
```

- ถ้าได้เป็นไฟล์ zip ให้แตกไฟล์ แล้วเปิดโฟลเดอร์โปรเจกต์ `CARPOOL/`

### 3) สร้าง Virtual Environment และติดตั้งแพ็กเกจ
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

> ถ้าเครื่องบล็อค script ใน PowerShell ให้รัน (ครั้งเดียว):
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`

### 4) ตั้งค่า Django (Database + Superuser)
```powershell
python manage.py migrate
python manage.py createsuperuser
```

### 5) รันระบบแบบ Dev
```powershell
python manage.py runserver
```

เข้าใช้งาน:
- `http://127.0.0.1:8000/`

---

## B) ติดตั้งแบบ Production บน Windows Server (Waitress + Service)

### 1) เตรียมไฟล์ตั้งค่า (แนะนำ)
เพื่อความปลอดภัย แนะนำแยกค่าคอนฟิกออกจากโค้ด เช่น `.env` (หรือ Environment Variables)

ค่าที่ควรมีอย่างน้อย:
- `SECRET_KEY`
- `DEBUG=False`
- `ALLOWED_HOSTS` (เช่น ชื่อเครื่อง/โดเมน)
- (ถ้ามี) ค่าพาธโฟลเดอร์เก็บไฟล์/รายงาน/backup

> หากโปรเจกต์ยังไม่ได้ทำ `.env` สามารถเริ่มจากแก้ใน `carpool_site/settings.py` ให้เหมาะกับ Production:
> - ปิด `DEBUG`
> - ตั้ง `ALLOWED_HOSTS`
> - ตั้งค่า static/media ตามโครงสร้างจริง

### 2) ติดตั้งแพ็กเกจสำหรับ Production
ให้ทำขั้นตอน venv + requirements เหมือน Developer และติดตั้งเพิ่ม (ถ้ายังไม่มีใน requirements):

```powershell
pip install waitress
```

> แนะนำให้ใส่ `waitress` ใน `requirements.txt` เพื่อความสม่ำเสมอ

### 3) Collect Static
```powershell
python manage.py collectstatic
```

ตรวจว่า `STATIC_ROOT` ถูกตั้งไว้แล้วใน `settings.py` (ทีมจะใช้ path ตามเซิร์ฟเวอร์จริง)

### 4) สร้างไฟล์รัน Waitress
สร้างไฟล์ `run_waitress.py` ที่ root เดียวกับ `manage.py`

```python
from waitress import serve
from carpool_site.wsgi import application

if __name__ == "__main__":
    # ปรับ host/port ตามต้องการ (เช่น 0.0.0.0:8000 หรือ 0.0.0.0:8080)
    serve(application, host="0.0.0.0", port=8000)
```

ทดสอบรัน:
```powershell
python run_waitress.py
```

จากเครื่องอื่นในเครือข่ายลองเข้า:
- `http://<SERVER-IP>:8000/`

> ถ้าเข้าไม่ได้ ให้เช็ค Windows Firewall (เปิดพอร์ต 8000) หรือใช้พอร์ตที่องค์กรอนุญาต

### 5) เปิดพอร์ตใน Windows Firewall (ถ้าจำเป็น)
ตัวอย่างเปิดพอร์ต 8000:
```powershell
netsh advfirewall firewall add rule name="Carpool 8000" dir=in action=allow protocol=TCP localport=8000
```

### 6) ทำให้รันอัตโนมัติเป็น Windows Service (แนะนำ: NSSM)

แนวทางที่นิยมบน Windows คือใช้ **NSSM (Non-Sucking Service Manager)** เพื่อรัน Python script เป็น Service

**ขั้นตอนโดยย่อ**
1. ดาวน์โหลด NSSM และแตกไฟล์ไว้ในเครื่อง (เช่น `C:\nssm\`)
2. เปิด CMD/PowerShell แบบ Run as Administrator
3. สร้าง Service

```powershell
#กรณี ยังไม่ได้ add NSSM เข้า PATH
##สมมติว่าไฟล์อยู่ที่ C:\nssm\nssm.exe
C:\nssm\nssm.exe install CarpoolService

#กรณี add NSSM เข้า PATH แล้ว
nssm install CarpoolService

# ตรวจสอบสถานะ service
nssm status CarpoolService

# เปิดหน้าตั้งค่า service (กรณีต้องแก้ path)
nssm edit CarpoolService

จากนั้นจะมีหน้าต่างให้ใส่ค่า:
- **Path**: พาธ `python.exe` ใน venv เช่น  
  `D:\CARPOOL\venv\Scripts\python.exe`
- **Startup directory**: โฟลเดอร์โปรเจกต์ `D:\CARPOOL`
- **Arguments**: `run_waitress.py`

กด Install service

เริ่ม/หยุด service:
```powershell
net start CarpoolService
net stop CarpoolService
```

> แนะนำให้ตั้ง Log file (stdout/stderr) ใน NSSM เพื่อดู error ง่ายขึ้น

### 7) (ทางเลือก) ใช้ IIS เป็น Reverse Proxy
ถ้าองค์กรต้องการให้เข้าผ่านพอร์ต 80/443 และมี SSL:
- ให้ Waitress รันหลังบ้าน (เช่น port 8000)
- IIS ทำหน้าที่ Reverse Proxy ไปที่ `http://127.0.0.1:8000`
- วิธีนี้ช่วยจัดการ SSL/โดเมน/การเข้าถึงได้ดีขึ้น

---

## C) ข้อแนะนำด้านความปลอดภัยและการดูแลข้อมูล

- ปิด `DEBUG` เสมอใน Production
- ตั้งค่า `SECRET_KEY` ใหม่สำหรับ Production และเก็บให้ปลอดภัย
- Backup ฐานข้อมูลเป็นประจำ
  - ถ้าใช้ SQLite: backup ไฟล์ `db.sqlite3`
  - ถ้าใช้ DB Server: ใช้เครื่องมือ backup ตาม DB นั้น ๆ
- หากมีการแก้ template รายงาน (`booking/report_templates/`) ควรทดสอบ export ทุกครั้งก่อนขึ้นระบบจริง

---

จัดทำโดย: ผู้พัฒนาระบบ
